import { useEffect, useState } from "react";

const isLoading = true;

const Header = () => {
  

  return (
    <button type="submit" class="bg-secondary-3 text-primary-1 font-bold py-4 px-12 rounded">Submit</button>
  );
}

export default Header;
